system:
  mode: "simulation" # Options: "simulation", "live"
  persistence: "postgres" # Options: "postgres"
  event_bus: "postgres" # Options: "postgres"

services:
  # Options for all services: "mock", "live"
  ceo: "mock"
  shop: "mock"
  ads: "mock"
  trends: "mock"
  competitor: "mock"
  fulfilment: "mock"
  email: "mock"
  ai: "live"
  operations: "mock"
  analytics: "mock"

paths:
  infrastructure: "config/infrastructure.yaml"
  mcp: "config/mcp.yaml"
  agents: "config/agents.yaml"
  workflows: "config/workflows.yaml"
